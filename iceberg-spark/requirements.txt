# Python dependencies for the Spark 4.1 Declarative Pipelines examples
#
# Install with:
#   pip install -r requirements.txt
#
# The Iceberg runtime JAR is NOT a pip package and must be built from source.
# See README.md (Prerequisites) for instructions.

pyspark[pipelines]==4.1.*
